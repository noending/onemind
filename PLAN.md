# 佛经/咒语科学记忆产品需求方案

## Summary

打造一个先以微信小程序验证、后续再延展到独立 App 的修行记忆产品。首发版本聚焦普通在家修行者，核心价值不是泛内容陪伴，而是帮助用户用更轻松的方式背下短咒、短偈和常用经文片段。

MVP 只做一条主闭环：`选内容 -> 系统自动生成记忆方案 -> 按记忆曲线提醒复习 -> 通过拆段训练真正背会`。佛菩萨诞辰、成道日内容作为轻量增强项接入；音乐、唐卡、能量画、数字资产管理不进入首发范围。

## Key Changes

### 多端工程路线

- 本项目采用微信开发者工具生成的多端 MiniApp 架构，不使用 `uni-app` 或 `Taro` 作为主路线。
- 业务代码按原生小程序方式组织：
  `WXML / WXSS / JS / JSON`。
- 多端支持依赖现有配置：
  `projectArchitecture: multiPlatform`、`mini-ios`、`mini-android`、`mini-ohos`。
- 首期优先跑通微信小程序模拟器，再验证 iOS、Android、HarmonyOS / OHOS MiniApp 容器。
- 端差异集中在提醒、音频、隐私权限、启动图标和系统能力适配层，核心记忆逻辑保持跨端一致。

### 产品定位与首发边界

- 首发产品定义为“记忆教练型”工具，不做综合佛学内容平台。
- 首发载体采用“小程序先验证，App 后承接”的路线。
- 首发内容仅支持短内容：
  短咒、短偈、常用经文片段、节日精选内容。
- 首发不支持完整长经逐章训练，只为后续 7 到 14 天计划预留能力。
- 首发不纳入：
  重商业化、数字资产体系、上师内容资产管理。

### 核心功能结构

- 首页：
  展示今日待复习内容、连续坚持天数、快背会内容、节日精选入口。
- 内容选择页：
  每条内容显示长度、预计背会天数、适合使用场景。
- 智能记忆方案：
  用户选中内容后，系统直接给出方案，用户一键接受即可开始。
- 训练方式默认采用“渐进拆段式”：
  分句拆段、首字提示、遮挡回忆、填空复现、音频跟读辅助。
- 提醒策略默认采用系统自动排程：
  小程序阶段以订阅消息、站内待办、首页提醒为主。
- 进度页：
  区分“复习中 / 已背会 / 遗忘风险高”的内容状态。
- 节日增强模块：
  在特定佛菩萨诞辰、成道日推荐对应内容，并支持一键加入记忆计划。

### 核心产品规则

- 按内容长度自动分配计划档位：
  `短内容 1-3 天`，`中内容 3-7 天`，`较长内容 7-14 天（预埋）`。
- 每个记忆计划必须输出明确的每日任务，不让用户自己配置复杂参数。
- 每次复习必须有结果反馈：
  更熟、已掌握、需加强，避免沦为纯提醒或纯打卡。
- 完成一段内容后，系统自动推荐下一段相关内容，形成连续学习链路。

### 关键产品对象 / 接口约束

- `ContentItem`
  字段至少包含：标题、类别、正文、长度等级、节日关联、推荐场景、音频资源状态。
- `MemoryPlan`
  字段至少包含：内容 ID、计划档位、起始日期、复习节点、当前阶段、完成状态。
- `ReviewTask`
  字段至少包含：计划 ID、任务日期、训练方式、完成结果、耗时。
- `FestivalRecommendation`
  字段至少包含：节日名称、日期、关联佛菩萨、推荐内容、推荐理由。
- `ProgressState`
  至少支持：复习中、已背会、高遗忘风险。

## Test Plan

### 用户场景验证

- 新用户首次进入后，能在 1 分钟内选定一段短咒并开始第一个记忆计划。
- 用户接受系统方案后，不需要手动配置频率，也能理解“接下来什么时候复习”。
- 用户完成首次训练后，能清楚看到自己是“学过了”还是“快背会了”。
- 用户连续 3 天按提醒复习后，能感知到进度累计，而不是只有签到记录。
- 节日当天进入产品时，能看到与当日相关的经文/咒语推荐，并能直接加入计划。

### 规则与边界验证

- 短内容应生成 1 到 3 天计划，中内容应生成 3 到 7 天计划。
- 超出首发支持范围的长内容，产品应提示“暂不支持完整训练”，而不是给出糟糕体验。
- 用户漏掉一天复习后，系统应能继续安排后续任务，不让计划直接失效。
- 用户完成某段内容后，系统应推荐下一段相近内容，而不是结束在空状态。

### 成功指标建议

- 首日创建记忆计划转化率。
- 3 日连续复习完成率。
- 单段内容背会完成率。
- 完成首段后继续添加第二段内容的比例。
- 节日推荐内容被加入计划的转化率。

## Assumptions

- 默认用户是普通在家修行者，不以寺院组织或弟子体系为首发客群。
- 默认首发主价值是“轻松背下来”，不是泛内容阅读或文化浏览。
- 默认提醒策略由系统自动排程，用户不需先做复杂设置。
- 默认节日能力只是主闭环增强项，不与记忆功能并列抢主叙事。
- 默认商业化后置：
  先验证留存，再扩展会员计划、专题内容包、音频与视觉内容。
- 默认数字资产管理属于后续独立业务线，不进入 MVP。
